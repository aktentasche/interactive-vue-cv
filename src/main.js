import { createApp } from "vue";
import App from "./App.vue";
import vuetify from '@/plugins/vuetify'

createApp(App).mount("#app");

app.use(vuetify, i18nStrings)
app.use(vuetify, i18nStrings)

new Vue({
    vuetify,
  }).$mount('#app')